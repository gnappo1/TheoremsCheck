<ul>
  <% if !@subjects.empty? %>
    <% @subjects.each do |s| %>
        <h3><%= link_to s.name, s %></h3><br>
    <% end %>
  <% else %>
    <h3>Currently there are no Subjetcs added from you</h3>
  <% end %>
</ul>
