$('body').off('click');
if( $("#edit-quote-<%= @quote.id%>-div").children().length == 0 ) {
  $("#edit-quote-<%= @quote.id%>-div").html("<%= j render 'quotes/form', quote: @quote %>");
} else {
  $("#edit-quote-<%= @quote.id%>-div").toggle();
}
