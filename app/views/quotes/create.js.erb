var html = "<%= j render 'quotes/quote_summary', q: @quote %>"
if (document.getElementById("empty")) {
  $('h3#empty').replaceWith(html)
  $('#form_for_quote').hide();
} else {
  $("div.all-quotes ul").append(html)
  $('#form_for_quote').hide();
}
