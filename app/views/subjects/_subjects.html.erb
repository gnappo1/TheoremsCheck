<div class="frame">
  <div class="subjects">
    <ul class="subjects-list">
      <% @subjects.each do |s| %>
        <%= render partial: 'subject', locals: {s: s} %>
      <% end %>
    </ul>
  </div>
</div>
