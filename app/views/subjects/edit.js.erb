$('body').off('click');
if( $("#edit-subject-<%= @subject.id%>-div").children().length == 0 ) {
 $("#edit-subject-<%= @subject.id%>-div").html("<%= j render 'subjects/form', subject: @subject %>");
} else {
  $("#edit-subject-<%= @subject.id%>-div").toggle();
}
