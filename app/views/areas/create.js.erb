var html = "<%= j render 'subjects/area', a: @area %>"
if (document.getElementById("empty-list")) {
  $('h3#empty-list').replaceWith(html)
  $('#area_name').val('');
} else {
  $("div#areas ul").append(html)
  $('#area_name').val('');
}
